# --- Supabase -----------------------------------------------------------
# Sans ces deux variables l'app tourne quand même : elle bascule sur un
# store en mémoire (dev uniquement, tout est perdu au redémarrage).
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Phase 2 (moteur IA) — pas encore utilisé ---------------------------
# ANTHROPIC_API_KEY=

# --- Phase 3 (affiliation / admin) — pas encore utilisé -----------------
# AMAZON_BR_TAG=
# MERCADO_LIVRE_TAG=
# MAGALU_TAG=
# SHOPEE_TAG=
# ADMIN_PASSWORD=
